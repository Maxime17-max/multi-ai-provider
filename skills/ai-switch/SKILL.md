---
name: ai-switch
description: Switch the active AI provider and model for the multi-provider gateway
---

Use the `ai_switch_active` tool.

1. Call `ai_provider_list` to show available providers
2. Call `ai_model_list` with the chosen provider to see models
3. Call `ai_switch_active` with the chosen provider and model
4. Confirm the switch to the user