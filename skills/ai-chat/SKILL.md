---
name: ai-chat
description: Chat with a specific AI provider and model from the multi-provider gateway
---

Use the `ai_chat` tool to send a message.

1. If no provider/model specified, use the currently active one
2. Call `ai_chat` with the user's message and optional provider/model
3. Return the response to the user