{
  "name": "multi-ai-provider",
  "version": "0.1.0",
  "description": "Multi-provider AI gateway for Claude Code — route requests to OpenAI, Anthropic, Gemini, OpenRouter, Ollama, LM Studio and more",
  "author": "Community",
  "license": "MIT",
  "mcpServers": {
    "ai-gateway": {
      "command": "npx",
      "args": ["tsx", "${CLAUDE_PLUGIN_ROOT}/servers/ai-gateway/src/index.ts"],
      "env": {
        "PLUGIN_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}